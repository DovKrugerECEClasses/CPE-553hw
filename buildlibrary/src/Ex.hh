class Ex {
private:
	static const char** messages;
	static void loadErrors(const char filename[]);
public:
	Ex(const char filename[], uint32_t linenum, uint32_t message) : 
};

enum MyErrors {
	ERR_CANNOT_OPEN,
	ERR_PERMISSION_DENIED,
	
	
};
